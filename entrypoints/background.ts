export default defineBackground(() => {
  // Placeholder — message routing and the session-swap mechanism are built
  // in Stage 4 (feature/background-worker), not this scaffolding stage.
  console.log('Store Switcheroo background service worker started');
});
